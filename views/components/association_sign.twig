{% set approved = reversed ? '&#x21C7;' : '&#x21C9;' %}{% set not_approved = reversed ? '&larr;' : '&rarr;' %}{% set ethereal = reversed ? '&#x21E0;' : '&#x21E2;' %}{% if association %}{{ (association.is_approved ? approved : not_approved)|raw }}{% else %}{{ ethereal|raw }}{% endif %}
