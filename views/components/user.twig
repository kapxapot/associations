<img src="{{ user.gravatar_url }}" class="avatar avatar--inline{% if big %} avatar--big{% endif %}" title="{{ user.display_name }}{% if not user.is_mature %} (ребенок!){% endif %}"  data-toggle="tooltip">{% if full %} <span>{{ user.display_name }}{% if not user.is_mature %} <i class="fas fa-child" title="Ребенок" data-toggle="tooltip"></i>{% endif %}</span>{% endif %}