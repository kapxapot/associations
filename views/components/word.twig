{% set utterance = utterance ?? word.originalUtterance %}{% if show_lock and word.hasActualOverride %}<i class="fas fa-lock gray-9"></i> {% endif %}<a href="{{ word.url }}"{% if show_typo and word.typo_by_me %} title="Оригинальное слово: {{ word.word }}" data-toggle="tooltip" data-placement="bottom"{% endif %}>{{ show_typo ? word.personalized_name : word.display_name }}{% if show_utterance and utterance and utterance != word.word %} {{ '{' ~ utterance ~ '}' }}{% endif %}</a>