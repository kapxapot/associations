{% if show_lock and word.hasActualOverride %}<i class="fas fa-lock gray-9"></i> {% endif %}<a href="{{ word.url }}"{% if word.typo_by_me %} title="Оригинальное слово: {{ word.word }}" data-toggle="tooltip" data-placement="bottom"{% endif %}>{{ word.display_name }}</a>{% if show_utterance and word.hasDifferentOriginalUtterance %} {{ '{' ~ word.originalUtterance ~ '}' }}{% endif %}