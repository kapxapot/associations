$scope.scopes = [
    {% for id, name in scopes %}
        {
            id: {{ id }},
            name: '{{ translate(name, obj)|capitalize }}'
        }{% if not loop.last %},{% endif %}
    {% endfor %}
];
