$scope.severities = [
    {% for id, name in severities %}
        {
            id: {{ id }},
            name: '{{ translate(name, obj)|capitalize }}'
        }{% if not loop.last %},{% endif %}
    {% endfor %}
];
